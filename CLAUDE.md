@AGENTS.md


## Active Technologies
- Kotlin (new code), targeting Java 17 toolchain + AndroidX RecyclerView, existing `AssetImageLoader` (LRU cache), (001-image-gallery)
- No new storage; images are pre-packaged assets in `assets/celestial_images/` (001-image-gallery)

## Recent Changes
- 001-image-gallery: Added Kotlin (new code), targeting Java 17 toolchain + AndroidX RecyclerView, existing `AssetImageLoader` (LRU cache),
